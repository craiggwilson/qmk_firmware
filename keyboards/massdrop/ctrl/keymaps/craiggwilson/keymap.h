#include QMK_KEYBOARD_H

enum layout_names {
    _TL=0, // Typing Layout: The main keyboard layout for typing.
    _FL,   // Function Layout: The function key activated layout with default functions and some added ones
    _NL,   // Navigation Layout: brings the arrow keys to WASD and the mouse to UIOJKL
};

